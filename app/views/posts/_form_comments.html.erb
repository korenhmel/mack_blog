 <%= form_for ([@post, @post.comments.build]) do |f|%>
  <%= f.label :title %><br />
  <%= f.text_field :title %><br />
  <%= f.label :body %><br />
  <%= f.text_area :body %><br />
  <%= f.submit "Create Comments" %>
<% end %>
